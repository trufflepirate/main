package seedu.address.logic.commands.job;

/**
 * Finds and lists all jobs in address book whose name contains any of the argument keywords.
 * Keyword matching is case insensitive.
 */
public class FindJobCommand {
}
